# Screenshots

Place screenshots here so they render in the root `README.md`:

- `dark-theme.png` — the default modern dark workspace (DES + GP side by side)
- `amber-theme.png` — the classic Bloomberg amber-on-black toggle
- `chart-indicators.png` — a price chart with volume + RSI + MACD panes

To capture: run the app (`npm run dev`), load a liquid ticker (e.g. `AAPL` or `NVDA`),
and screenshot the browser at a desktop width (≥1440px) for the best density.
