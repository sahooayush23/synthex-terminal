# Leave empty for local dev — Vite proxies /api to http://localhost:8000.
# In production (Vercel) set this to your deployed backend, e.g.
#   VITE_API_URL=https://synthex-api.onrender.com
VITE_API_URL=
